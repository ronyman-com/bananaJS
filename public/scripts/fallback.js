// Fallback JavaScript
console.log('CDN script failed to load. Using local fallback.');

// Example fallback functionality
document.addEventListener('DOMContentLoaded', () => {
  console.log('Fallback script loaded successfully.');
});